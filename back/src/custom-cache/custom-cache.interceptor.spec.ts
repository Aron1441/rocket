import { CustomCacheInterceptor } from './custom-cache.interceptor';

describe('CustomCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new CustomCacheInterceptor()).toBeDefined();
  });
});
